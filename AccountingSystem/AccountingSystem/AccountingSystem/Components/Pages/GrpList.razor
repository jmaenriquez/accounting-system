@page "/accgrp"

@inject NavigationManager Navigate

<PageTitle>Account Groups</PageTitle>

<div class="rounded-3">

    <div class="top-container">

        <h3 class="m-4">Account Groups</h3>

<<<<<<< Updated upstream
        <div class="buttons">
            <input class="searchbar" type="text" placeholder="Search..." />
            <button class="btn add" @onclick="navigateToAddGrp">Add New</button>
=======
        <div class="buttons" style="display: flex; align-items: center;">

            <MudHidden Breakpoint="Breakpoint.SmAndDown">
                <input class="searchbar" type="text" placeholder="Search..." />
            </MudHidden>

            <MudHidden Breakpoint="Breakpoint.MdAndUp">
                <div class="mobile-search">
                    <MudButton Variant="Variant.Filled" OnClick="ToggleSearch" Style=" width:fit-content; background-color:#00828F; color:white; margin-left: 10px;">
                        <MudIcon Class="search-icon" Icon="@Icons.Material.Filled.Search" />
                    </MudButton>
                    <input class="search-input @(isSearchOpen ? "expanded" : "")" type="text" placeholder="Search..." autofocus />
                </div>
            </MudHidden>

            <MudButton Variant="Variant.Filled" OnClick="OpenAddGrpDialog" Style="width:fit-content; background-color:#00828F; color:white; margin-left: 10px;">
                <MudHidden Breakpoint="Breakpoint.SmAndDown">
                    <MudIcon Icon="@Icons.Material.Filled.Add" /> New Entry
                </MudHidden>
                <MudHidden Breakpoint="Breakpoint.MdAndUp">
                    <MudIcon Icon="@Icons.Material.Filled.Add" />
                </MudHidden>
            </MudButton>
>>>>>>> Stashed changes
        </div>
    </div>

    <table class="mt-3 table table-borderless table-sm">
        <thead class="table-info ">
            <tr>
                <th>Date</th>
                <th>Account Id</th>
                <th>Account Name</th>
                <th>Description</th>
                <th>Account Type</th>
                <th>Status</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>

            @foreach (var grp in GroupList)
            {
                <tr>
                    <td>@grp.datetime</td>
                    <td>@grp.id</td>
                    <td>@grp.name</td>
                    <td>@grp.description</td>
                    <td>@grp.type</td>
                    <td>@grp.status</td>
                    <td>Actions</td>
                </tr>
            }
            
        </tbody>
    </table>
</div>


@using AccountingSystem.Data
@using AccountingSystem.Services
@inject GroupService grpService

@code {

<<<<<<< Updated upstream

=======
    private bool isSearchOpen = false;

    private void ToggleSearch()
    {
        isSearchOpen = !isSearchOpen;
    }
>>>>>>> Stashed changes

    private void navigateToAddGrp()
    {
        Navigate.NavigateTo("/addgrp");
    }

    List<Group_List> GroupList = new List<Group_List>();
    List<Group_List> FilteredEntries = new List<Group_List>();

     protected override async Task OnInitializedAsync()
    {
        await LoadAccounts();
        await Refresh();

    }

    private async Task LoadAccounts()
    {
        GroupList = (await grpService.GroupItems()).ToList();
        StateHasChanged();
    }

    private async Task Refresh()
    {
        while (true)
        {
            await LoadAccounts();
        }
    }
}
